const { getUser, updateUser } = require('./user');
const { getProfile, updateProfile, deleteProfile } = require('./profile');

module.exports = { getUser, updateUser, getProfile, updateProfile, deleteProfile };
